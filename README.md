# Phils Helper Methods

This extension provides:

- **Given/When/Then snippets** for easy test writing.
- **Automatic file generation**: When you create a folder for specified files, it will generate a barrel file, a test file, and a component file inside.

**Enjoy!**
